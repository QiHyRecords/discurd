# Copy to android/keystore.properties locally or let GitHub Actions create it from secrets.
# Do not commit the real keystore.properties file.
storeFile=lumalink-release.jks
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=lumalink
keyPassword=REPLACE_WITH_KEY_PASSWORD
